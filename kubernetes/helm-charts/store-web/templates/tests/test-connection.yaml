apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "store-web.fullname" . }}-test-connection"
  labels:
    {{- include "store-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "store-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
