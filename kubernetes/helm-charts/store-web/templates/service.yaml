apiVersion: v1
kind: Service
metadata:
  name: {{ include "store-web.fullname" . }}
  labels:
    {{- include "store-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "store-web.selectorLabels" . | nindent 4 }}
