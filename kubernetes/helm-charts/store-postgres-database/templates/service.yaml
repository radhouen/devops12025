apiVersion: v1
kind: Service
metadata:
  name: {{ include "store-postgres-database.fullname" . }}
  labels:
    {{- include "store-postgres-database.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "store-postgres-database.selectorLabels" . | nindent 4 }}
